# 💾 Гайд збереження даних

## Нові можливості

### 1. Збереження реєстрації
- Користувачі зберігаються в `data/users.json`
- При перезапуску бота користувачі залишаються зареєстровані
- Автоматичне завантаження при старті

### 2. Запис завдань
- Завдання зберігаються в `data/tasks.json`
- Кожне завдання має: user_id, текст, статус, час
- Історія всіх завдань

### 3. Запис в Windsurf
- Завдання автоматично пишуться в `data/windsurf_tasks.txt`
- Копіюється текст завдання
- Включає час та ID користувача

---

## 📁 Структура даних

### data/users.json
```json
{
  "123456": {
    "username": "john_doe",
    "user_id": 123456,
    "authenticated": true
  }
}
```

### data/tasks.json
```json
{
  "1": {
    "user_id": 123456,
    "task": "напиши Hello World",
    "status": "pending",
    "timestamp": "2025-11-21T16:19:17.652345"
  }
}
```

### data/windsurf_tasks.txt
```
============================================================
[2025-11-21 16:19:17] User 123456
Task: напиши Hello World
============================================================
```

---

## 🚀 Як працює

### При реєстрації:
1. Користувач вводить `/register Ml120998`
2. Система перевіряє пароль
3. Користувач зберігається в `data/users.json`
4. При перезапуску бота користувач залишається зареєстрований

### При завданні:
1. Користувач вводить `/task напиши Hello`
2. Завдання зберігається в `data/tasks.json`
3. Завдання копіюється в `data/windsurf_tasks.txt`
4. Бот показує опис завдання
5. Користувач підтверджує

---

## 💡 Приклади

### Приклад 1: Перезапуск бота

**Сеанс 1:**
```
/register Ml120998
✅ Ви зареєстровані як 'username'!
```

**Перезапуск бота...**

**Сеанс 2:**
```
/login Ml120998
✅ Ви аутентифіковані як 'username'!
```

Користувач залишився зареєстрований!

### Приклад 2: Завдання в Windsurf

```
/task напиши важливий текст
```

**data/windsurf_tasks.txt:**
```
============================================================
[2025-11-21 16:19:17] User 123456
Task: напиши важливий текст
============================================================
```

---

## 🔧 Функції

### save_users(users_dict)
Зберігає користувачів в JSON

```python
from persistence import save_users
save_users(users_dict)
```

### load_users()
Завантажує користувачів з JSON

```python
from persistence import load_users
users = load_users()
```

### save_task(user_id, task_text, status)
Зберігає завдання

```python
from persistence import save_task
task_id = save_task(123456, "напиши Hello", "pending")
```

### write_to_windsurf(task_text, user_id)
Пише завдання в Windsurf файл

```python
from persistence import write_to_windsurf
write_to_windsurf("напиши Hello", 123456)
```

### get_all_tasks()
Отримує всі завдання

```python
from persistence import get_all_tasks
tasks = get_all_tasks()
```

---

## 📊 Переваги

✅ **Персистентність** - дані зберігаються
✅ **Історія** - всі завдання записуються
✅ **Windsurf інтеграція** - завдання копіюються
✅ **Автозавантаження** - користувачі завантажуються при старті
✅ **JSON формат** - легко читати та редагувати

---

## ⚠️ Важливо

- Папка `data/` створюється автоматично
- JSON файли кодуються в UTF-8 (підтримка української)
- Всі операції логуються в `logs/system.log`

---

## ✨ Готово!

Система має повну персистентність даних!

```bash
py main.py
```
